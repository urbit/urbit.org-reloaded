# urbit.org

The website for the Urbit project.

## Build

1. git clone this repository
2. Install [zola](https://www.getzola.org/documentation/getting-started/installation/)
3. `cd` into `content/docs` and run `git pull`
4. In the root directory of `urbit.org`, run `zola build` or `zola serve`